# ifndef TOOLS_FILE
# define TOOLS_FILE

# include <string>

const size_t TABLE_MAX_PAGE = 100;

// one block of memory per page
const uint32_t PAGE_SIZE = 4096;

bool isANumber (const string& token);

# endif

